While the main strategy for signal extraction uses multivariate algorithms,
we have optimised a basic selection using standard discriminating variables
that can be used for increasing the signal-over-background ratio for various studies.

% - Description of the background
The background is made up of two distinctly-behaving components: combinatorial background
and misreconstructed B background. The combinatorial background arises from the random
combination of unrelated tracks. The misreconstructed B decays are decays involving
B mesons, in which one or more of the final state particles are misidentified
or missing.
After preselection, the background events consist overwhelmingly of the combinatorial
component and most of these events have rather different properties with respect to
the signal. Hence few basic selections can greatly reduce the amount of background
feeding in our working sample and with reduced numbers we can explore and cross-check
better our data and our MC samples.

A Continuum Reduction Selection (CRS), which uses lifetime-related variables
that can clean up the overwhelming combinatorial background, is developed.

% - The variables used in the CRS and their definitions
The main effect we can exploit is the fact that B decays have a longer lifetime
and larger values for other variables proportional to the lifetime.
The variables considered in the CRS are:
$\tau_{B}$ -- the lifetime of the B, $L_{\textrm{xy}}$ -- the displacement of the $J/\psi$
vertex from the PV, projected onto the flight direction of the $J/\psi$
in the transverse plane, and $\cos \theta$ -- the cosine of the angle between the vector
joining the PV and SV and the direction of flight of the B. The significance values
(i.e. the variable divided by its error) of these variables are also considered in the
optimisation of the CRS.

% - CRS optimisration incl. fits
To optimise our cuts, the Punzi significance~\cite{Punzi} is used as it relies on the
signal efficiency rather than the number of signal events (like other figures of merit
such as $Z = S/\sqrt{S+B}$).
The Punzi significance is used when we are uncertain of the cross-section we should expect,
which will inevitably be factored in when we want to give an estimate for the signal yield.
For the $R(K^{*})$, the large uncertainties on the $b\Bar{b}$ cross-section make the use
of the Punzi significance preferable. The signal efficiency of the selection to be
optimised is calculated from the percentage of the signal MC events passing it over the
total number of events we start with after the preselection.
Explicitly, the Punzi significance is calculated as
\begin{equation}
    Z_{\textrm{Punzi}} = \frac{\epsilon(t)}{a/2 + \sqrt{B(t)}},
    \label{eq:punzicalc}
\end{equation}
where $\epsilon(t)$ is the signal efficiency, $a$ the number of sigma deviations
(corresponding to one-sided Gaussian test, at some significance $\alpha$),
and $B(t)$ the number of background events passing the cut, at threshold $t$.
This also allows us to optimise the cuts without knowing the form of the signal
in the data \textit{a priori}, and to optimise our cuts on the blinded signal
significance in the low-$q^{2}$ region.

\begin{figure}[]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/SidebandFit_CRSoptimisation.png}
    \caption{A blinded extended-likelihood fit to the invariant mass in the
    low-$q^{2}$ signal region. Here the background shape is modelled by an exponential
    (combinatorial component) and a wide Gaussian (misreconstructed B component).
    Below the pulls for the fit are also shown.}
    \label{fig:SBFit}
\end{figure}

\begin{figure}[]
    \centering
    \includegraphics[width=0.45\textwidth]{figures/CRSVarCorrs.png}
    \includegraphics[width = 0.44\textwidth]{figures/PunziScan.png}
    \caption{{\textit{Right plot}}: The linear correlation coefficients between the various
    CRS variables.
    {\textit{Left plot}}: Punzi significance scans for each of the $6$ variables in the CRS optimisation,
    here the $L_{\textrm{xy}}$ significance value has the peak Punzi significance.
    }
    \label{fig:CorrAndPunziScan}
\end{figure}

We choose two variables amongst the ones listed above to define the CRS, performing
a 2D optimisation.
The two selected variables must have both good discriminating power between combinatorial
events and misreconstructed B decays.
To choose the first of the two variables, a significance scan was performed
on each of the CRS variables and the Punzi significance calculated at each
point in the scan (corresponding to varying the cut of each CRS variable).
In calculating the Punzi significance, the $\epsilon(t)$ was calculated as
the fraction of signal MC passing the cut, the value of $a$ was taken as 2,
and the background was calculated from a fit to the blinded low-$q^{2}$
sideband data. Here, assuming the sideband is a good approximation to the background,
we can calculate the number of background events $B(t)$ passing the cut
by rescaling our signal yield in the sidebands by the fraction of the blinded fit yield
in the signal region and sideband region. The blinded signal region of the low-$q^{2}$
data is defined as $m_{B} \in [4.0, 5.7]$\,GeV, whereas the sideband is defined
as the region $m_{B} \in [3.5, 4.0] \cup [5.7, 6.5]$\,GeV. Fig. \ref{fig:SBFit}
shows the blinded fit to the low-$q^{2}$ invariant B mass distribution.
The signal-to-sideband normalisation ratio from the blinded fit background yield
is a factor of $0.820$. The denominator in Eq.~\ref{eq:punzicalc}
can then be expressed as $1 + \sqrt{0.820 B_{\textrm{SB}}(t)}$.

\begin{figure}[]
    \centering
    \includegraphics[width = 0.47\textwidth]{figures/2DPunziOpt.png}
    \includegraphics[width = 0.47\textwidth]{figures/2DPunziOpt_zoom.png}
    \caption{
    {\textit{Right plot}}: 2D optimisation performed by scanning the Punzi significance of
    $\cos \theta$ cuts, for a given cut on the $L_{\textrm{xy}}$ significance.
    {\textit{Left plot}}: zoomed-in version of the Punzi significance scan on the right.}
    \label{fig:PunziMax}
\end{figure}

For each of the Punzi significance scans, shown in Fig. \ref{fig:CorrAndPunziScan}, the peak
significance was taken. These were then compared and the CRS variable with the highest
peak Punzi significance was chosen as the first variable for optimisation in the CRS.
This is seen to be the $L_{\textrm{xy}}$ significance. It is worth noting that
the Punzi significance does not correspond to a physically meaningful significance,
nor a sensitivity, instead, its maximum corresponds to a maximumimally sensitive measurement,
i.e. by maximising the Punzi significance of our CRS scan we maximise our sensitivity
to the signal.

For choosing the second variable to be optimised, and with the idea of choosing the remaining
variable which would exploit the most information about the data, the linear correlations between
the CRS variables were calculated. The correlations between the variables are shown in
Fig.~\ref{fig:CorrAndPunziScan}.
Having chosen the $L_{\textrm{xy}}$ significance as the first variable in the 2D CRS optimisation,
the second variable is the one with the smallest correlation coefficient with $L_{\textrm{xy}}$,
which is seen to be $\cos \theta$.
Finally, the 2D optimisation of the cuts in $L_{\textrm{xy}}/\sigma_{L}, \cos \theta$
was done by performing a grid search maximisation of the Punzi significance for pairs of cuts.
Fig~\ref{fig:PunziMax} shows the Punzi significance scan for a few slices of the grid search.
Optimum cuts on $(L_{\textrm{xy}}/\sigma_{L}, \cos \theta/)$ were found
to be $L_{\textrm{xy}}/\sigma_{L} > 18.75, \cos \theta > 0.999938$.
\begin{figure}
    \centering
    \includegraphics[width = 0.4\textwidth]{figures/B_masses_cutflow_JPsi_highq2_datafit.png}
    \includegraphics[width = 0.4\textwidth]{figures/BmassFit_Jpsi_Data_Highq2_CRS_extended_Bkgpdf_erfc_cores_free.png}
    \caption{(Left) Extended likelihood fits to the invariant B mass, with only the preselection applied,
    here signal is shown in blue and background in red. The fit results are also shown.
    (Right) Extended likelihood fits to the invariant B mass, with the preselection and
    additional CRS applied, here signal is shown in blue and background in red.
    The fit results are also shown. }
    \label{fig:CRSvsCutflowFits}
\end{figure}

The optimised CRS was then applied to the low-$q^{2}$ sideband data and also high-$q^{2}$ data
to assess the effect of the optimised selection on the reduction of combinatorial background.
For the high-$q^{2}$, we see a clearer emergence of the $B$ mass peak from the overwhelming
background. Fig.~\ref{fig:CRSvsCutflowFits} shows extended likelihood fits to the invariant
mass of $B$ candidates, with and without the CRS applied. As can be seen, the CRS gives us
a much better handle on the functional form of the signal shape and allows us to better understand
the contribution of the misreconstructed B background.

\input{tex/table_crscutflow_electrons.tex}
