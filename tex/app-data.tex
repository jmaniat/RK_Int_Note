This appendix adds information to the signal Monte Carlo (MC), background MC and data samples
in Tables~\ref{app:tab:data:sig} and~\ref{app:tab:data:bkg} for signal-like decays
and background channels, respectively.
\input{tex/app-table_samples.tex}
